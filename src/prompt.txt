So please see here that we have a pretty nice setup, fully ready here. What it does is it downloads videos audio transcripts from YouTube and then, like, runs transcription and all that, transcription from models and all that, and saves the transcription to parsed conversations and all that. It's a very nice code base. But if you look closely, one interesting thing is that although we have a multimodal capability, I mean the YouTube videos obviously also allow downloading the audios, right? So if we want it, we could also store the audios here in the dataset folder. We could also store the audios here. However, we have not really done that yet. But, like, we surely could have done that, right? Yeah. So what I now need is, like, adding some more scripts. I mean, probably it would be best not to edit on existing scripts, rather add some more scripts as needed for audio handling, or youyu may syuurely edit scripts in here  no prooblem as long everhything works fine . Like probably, I don't know, here probably there are scripts for downloading the audio from scripts and transcribing and parsing and all that. So probably we can have one more script as of now for the audios. And one major problem that might be, like probably, like how do we know whether the audio for some model has already been downloaded or not? These are the things that you need to handle, so that redundancy is handled, yet it. Okay? So yeah, like if I run, if I run, so it will automatically check which audio have already been downloaded or not. So kind of like that, okay? Yeah. And another question is, where should the audios be downloaded? So if you look the structure, so inside the save data, save data, saved-data, that folder, we have a dataset folder, right? So inside going to that folder, the dataset folder, there we have the video ID, okay? Inside each video ID, we have a transcribe folder, right? A transcribe folder, then like here and all that. So what I want is, like, a new folder named Audio, a new folder named Audio as a sister to this transcribe folder, and inside that we will have the audio files downloaded for this particular video, right? So yeah, that's what I now want. And you can introduce more log files, obviously. Here are some log files, so you can introduce log files for audio. I mean, I want everything to be backward compatible, and I want everything to be just kind of, like, added a new layer, kind of like that, okay? So that everything underneath should still be working, and we are just adding the capability to work with audio seamlessly with no problem at all, okay? So yeah. So yeah, you have to implement this and verify this. But obviously verifying, I mean, all the audio would take an enormous amount of space in this local disk, so we do not buy one. So we'll obviously verifying just for only a handful number of videos, yeah.